//module.exports = function(sequelize, DataTypes) {
  //var Thread = sequelize.define("Thread", {
    // Field filled out by the user
   // subject: {
    //  type: DataTypes.STRING,
    //  allowNull: false,
    //  validate: {
     //   len: [1]
     // }
  //  //},
    // Pull in user name
   // user: {
     // type: DataTypes.TEXT,
     // allowNull: false,
    //  len: [1]
   // },
   // // Need to verify this is a possibility
   // timestamp: {
   //   type: DataTypes.STRING,
   //   defaultValue: "Personal"
  //  }
  //});
  //return Thread;
//};